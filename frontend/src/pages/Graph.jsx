export default function Graph() {
  return (
    <div>
      <h1>Graphe des collaborations</h1>
      <p>TODO: visualisation du graphe (ex: react-force-graph / vis-network) via GET /api/graph.</p>
    </div>
  );
}
